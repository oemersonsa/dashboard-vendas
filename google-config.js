window.DASHBOARD_GOOGLE_CONFIG = window.DASHBOARD_GOOGLE_CONFIG || {
  // Fallback para hospedagem puramente estatica.
  // Quando o app roda via server.js, este arquivo e sobrescrito dinamicamente pelo backend.
  clientId: "",
  backendBaseUrl: ""
};
